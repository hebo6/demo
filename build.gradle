subprojects {
    afterEvaluate {
        def rootRunTask = getTasksByName('bootRun', false)
        if (!rootRunTask.isEmpty()) {
            println rootRunTask
            bootRun {
                systemProperties "spring.output.ansi.enabled": "ALWAYS"
            }
        }
    }
}
